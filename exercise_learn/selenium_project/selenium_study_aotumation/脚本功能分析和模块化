

功能分析与模块化：


1.openbrowser   打开浏览器
    1) 打开浏览器，又需要关注一点，设置相应的驱动路径，以及不同的浏览器打开是否正常，可以简单做一下兼容性测试
    2) 封装成一个函数来调用，通过判断浏览器类型来设置驱动，调用不同浏览器的设置方法，例如：webdriver.Chrome()/webdriver.Firefox()/webdriver.Ie()等等

2.openurl    打开一个网址
    1) 打开一个网址，是否可以打开多个不同的网址呢


3.findelement   发现元素，定位元素


4.send_keys    找到相应的元素，并且发送相应的内容

5.checkresult   检查结果，断言结果



测试脚本模块化和数据分离：


